# 제주 길모아 — Agent 지침

## 프로젝트
React Native(Expo) + WebView 하이브리드 앱. 하단 탭바(홈/지도/계획/기록/마이) 중
홈/계획/기록/마이는 WebView로 React 웹앱을 로드하고, 지도 탭만 네이티브(네이버맵 SDK)다.

- 전체 아키텍처/단계별 계획: `docs/PLAN.md`
- 현재 진행 상황과 체크리스트: `docs/TASKS.md`
- 코드 컨벤션, 브릿지 프로토콜, 지도 SDK 주의사항: `.cursor/rules/*.mdc`

## 작업 시작 전 항상
1. `docs/TASKS.md`에서 현재 미완료 Phase/항목을 확인한다.
2. 그 Phase의 범위 밖 파일은 건드리지 않는다. 범위를 벗어나야 할 것 같으면 먼저 이유를 설명하고 확인받는다.

## 반드시 지킬 것
- 패키지 매니저는 pnpm을 사용한다 (`pnpm add` / `pnpm add -D`). 새 패키지 설치 전에는 어떤 패키지를 왜 쓰려는지 먼저 알리고 확인받는다 (자동 설치 금지).
- 네이버맵 SDK 등 확신 없는 외부 라이브러리 API는 추측해서 코드를 쓰지 않는다. TODO 주석을 남기고 공식 문서 확인이 필요하다고 알린다.
- 파일 수정 후 `pnpm exec tsc --noEmit`으로 타입 에러 여부를 스스로 확인하고 결과를 보고한다.
- 태스크를 끝낼 때마다 `docs/TASKS.md` 체크박스를 갱신한다.
- 네이티브 설정(app.config.ts, android/, ios/) 변경은 변경 사유를 요약해서 알린다.
- API 키/시크릿은 코드에 하드코딩하지 않고 `.env`를 통해서만 참조한다.

## 위험 명령
`rm -rf`, `git push --force`, `git reset --hard`, 배포/퍼블리시 명령은 `.cursor/hooks.json`이
시스템 레벨에서 차단한다. 차단되면 이유를 사용자에게 설명하고 직접 실행 여부를 물어본다.
